# Module 08 - Network isolation and mediated egress (scaffold)

Planned outcome: remove direct egress, expose synthetic local services through a broker, reauthorize redirects and DNS results, and bind requests to an explicit run identity.

Independent lab: reach an allowed randomized service while denying protected service, redirect, alternate-address, and direct-socket variants.
